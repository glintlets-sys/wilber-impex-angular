import { ConvertToWordsPipe } from './convert-to-words.pipe';

describe('ConvertToWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
